export const STORAGE_KEYS = {
  SESSION_ID:
    "insightflow_session_id",

  PENDING_QUEUE:
    "insightflow_pending_queue",
};

export const DEFAULTS = {
  FLUSH_INTERVAL:
    5000,

  BATCH_SIZE:
    20,
};